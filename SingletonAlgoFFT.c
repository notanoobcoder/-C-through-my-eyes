//Singleton's Algorithm
#include<stdio.h>
#include<conio.h>
#include<math.h>

void fourew(float iunit[],int na, int nb,int nc,int nd);
void main(){
int ndim,nn[ndim],isign,iunit[4],kbf=128;

float tempr,tempi,afa[kbf],afb[kbf],afc[kbf];
double wr,wi,wpr,wpi,wtemp,theta;
n=1;j=1;
do{
	n*=nn[j];
	if(nn[j]<=1)
		printf("Invalid dimension or wrong dimension");
	j++;
}while(j<=ndim);
nv=ndim;
jk=nn[nv];mm=n;
ns=n/kbf;nr=ns/2;kc=0;
kd=kbf/2;ks=n;
fourew(iunit,na,nb,nc,nd);
theta=pi/(isign*n/mm);



	getch();
}
